'use client';

export function AdminDashboard() {
  return (
    <div>
      {/* The dashboard content has been temporarily removed to resolve permission errors. */}
      {/* You can add back non-Firebase dependent statistics or content here. */}
    </div>
  );
}
